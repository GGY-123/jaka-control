# 总流程源码交付与环境依赖清单

## 1. 项目范围

本项目的总流程为：

1. 紫色试管取液：A5 取管、旋转夹爪夹持、JAKA Mini 吸取 600uL 并分三次排液，最后回到 A5 夹取点上方。
2. 橙色试管 1/2/3：A5 依次取管、取盖、旋盖、放回对应终点。

流程执行由后端严格串行控制：上一步运动或设备动作未完成时，不执行下一步；任一步失败或停止，后续步骤取消。

设备 IP 默认值：

| 设备 | 默认地址/接口 | 用途 |
|---|---|---|
| JAKA A5 | `192.168.1.102` | 紫色试管和橙色试管取放、旋盖 |
| JAKA Mini | `192.168.1.103` | 试管上方/下方运动、吸液和三次排液配合 |
| Z-ERG-20C 旋转夹爪 | `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0` | 夹持、旋转、松开试管 |
| ADP 移液枪 | USB-RS485 串口，常见 `/dev/ttyUSB1` | 吸液 600uL、排液 200uL x 3 |
| 末端夹爪（附属） | FTDI 串口，按实际设备识别 | A5 取管、取盖、放管 |

IP 和串口均可通过环境变量或网页配置调整，现场部署前必须以实际设备检测结果为准。

## 2. 源码交付包

交付根目录：`Demo/jakaA5/python——control/`

### 2.1 后端源码

| 文件 | 作用 |
|---|---|
| `server.py` | FastAPI 后端、普通流程、取液子流程、橙色三管子流程、总流程、设备协调和接口 |
| `jaka_arm.py` | JAKA Python SDK 包装，A5 和 Mini 共用 |
| `gripper.py` | 末端夹爪串口控制 |
| `pipette.py` | ADP 移液枪 RS485 ASCII/CRC 协议控制 |
| `adp_rs485_check（仅检查通讯是否正常的脚本）.py` | ADP 移液枪只读通信检查，只查询版本，不执行初始化、吸液或排液 |
| `adp_rs485_interactive（吸0.6ml分三批排0.2ml脚本）.py` | ADP 移液枪终端交互联调，支持初始化、Tip 检查、600uL 吸液和 200uL x 3 排液 |
| `points.json` | 点位库，包含 A5 和 JAKA Mini 点位 |
| `flows.json` | 网页保存的普通流程数据 |
| `requirements.txt` | Python 依赖清单 |
| `start_backend.sh` | 只启动后端的脚本 |
| `start_dashboard.sh` | 同时启动后端和前端的脚本 |
| `README_ADP_RS485.md` | 移液枪协议和串口说明 |

### 2.2 前端源码

目录：`frontend/`

| 文件/目录 | 作用 |
|---|---|
| `src/App.tsx` | 页面入口和顶部导航 |
| `src/components/FlowEditor.tsx` | 流程编辑器、流程库、统一控制栏 |
| `src/components/TransferSubflowPanel.tsx` | 紫色试管取液子流程 |
| `src/components/OrangeCappingSubflowPanel.tsx` | 橙色试管 1/2/3 子流程 |
| `src/components/CombinedSubflowPanel.tsx` | 紫色取液到橙色三管的总流程状态和日志 |
| `src/components/ManualControl.tsx` | 手动控制、移液枪和末端夹爪操作 |
| `src/components/ZergPanel.tsx` | 旋转夹爪 ROS2 状态和动作 |
| `src/components/Joystick.tsx` | 手动点动摇杆 |
| `src/components/WaypointManager.tsx` | 点位保存、编辑、预览、移动到点位 |
| `src/components/GripperPanel.tsx` | 手动控制页的末端夹爪操作 |
| `src/components/PipettePanel.tsx` | 手动控制页的移液枪操作 |
| `src/components/StatusPanel.tsx` | 机器人状态监控 |
| `src/components/CombinedSubflowPanel.tsx` | 紫色取液到橙色三管的总流程状态和日志 |
| `src/main.tsx` | React 应用挂载入口 |
| `src/ui.ts` | 通用按钮、卡片、输入框样式类 |
| `src/index.css` | 全局样式和 Tailwind 入口 |
| `src/api.ts` | 前端到 FastAPI 的请求封装 |
| `src/types.ts` | TypeScript 类型定义 |
| `package.json`、`package-lock.json` | Node.js 前端依赖 |
| `index.html` | Vite HTML 入口 |
| `vite.config.ts`、`tsconfig*.json` | Vite/TypeScript 构建配置 |
| `tailwind.config.js`、`postcss.config.js` | Tailwind/PostCSS 构建配置 |

`CoordinatedWorkflowPanel.tsx` 目前是旧的紫色到橙色协同面板，已从 `FlowEditor.tsx` 移除，不属于当前总流程运行必需文件；建议保留在源码包中，便于回滚或历史对照。

### 2.3 JAKA SDK

目录：`Demo/JAKA_Mini2_Python_Test/sdk/`

必须交付并保持可读文件：

- `jkrc.so`
- `libjakaAPI.so`
- `jkrc.pyi`

A5 和 Mini 使用同一套 SDK，区别只在登录 IP。不要分别复制成互相不一致的 SDK 版本。

### 2.4 ZERG SDK/ROS2 工作区

目录：`Demo/ZERG-SDK/`

必须交付：

- `zerg_sdk/`：Python SDK 和串口协议
- `zerg_ros2_driver/`：ROS2 驱动节点
- `zerg_interfaces/`：`OpenGripper`、`CloseGripper`、`RotateMotor` Action 定义
- `install/`：已构建的 ROS2 工作区（可重新构建）
- `build/`、`log/`：建议保留用于现场排查，发布包可按部署策略决定是否包含

ZERG 源码中总流程实际使用的关键文件：

- `zerg_sdk/zerg_sdk/__init__.py`
- `zerg_sdk/zerg_sdk/gripper.py`
- `zerg_sdk/zerg_sdk/protocol.py`
- `zerg_sdk/zerg_sdk/transport.py`
- `zerg_sdk/zerg_sdk/registers.py`
- `zerg_sdk/zerg_sdk/models.py`
- `zerg_sdk/zerg_sdk/exceptions.py`
- `zerg_sdk/setup.py`、`zerg_sdk/pyproject.toml`
- `zerg_ros2_driver/zerg_ros2_driver/driver_node.py`
- `zerg_ros2_driver/setup.py`、`zerg_ros2_driver/setup.cfg`、`zerg_ros2_driver/package.xml`
- `zerg_ros2_driver/config/zerg_driver.yaml`
- `zerg_ros2_driver/launch/zerg_driver.launch.py`
- `zerg_interfaces/action/OpenGripper.action`
- `zerg_interfaces/action/CloseGripper.action`
- `zerg_interfaces/action/RotateMotor.action`
- `zerg_interfaces/CMakeLists.txt`、`zerg_interfaces/package.xml`

### 2.5 移液枪独立联调脚本

这两个脚本不是网页后端必需模块，但属于移液枪交付包，用于脱离网页单独确认硬件和协议：

```bash
cd ~/Demo/jakaA5
python3 'adp_rs485_check（仅检查通讯是否正常的脚本）.py' --port /dev/ttyUSB1
python3 'adp_rs485_interactive（吸0.6ml分三批排0.2ml脚本）.py' --port /dev/ttyUSB1
```

脚本默认设备地址为 `02`、波特率为 `115200`；实际串口应以 `ls -l /dev/ttyUSB*` 和 `dmesg` 现场识别结果为准。

## 3. 软件环境依赖

推荐环境：Ubuntu 22.04、ROS2 Humble、Python 3.10、Node.js 18 或更高版本、npm。

### 3.1 系统工具

```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip \
  build-essential git curl usbutils pciutils lsof
```

### 3.2 ROS2

必须安装并能加载：

```bash
source /opt/ros/humble/setup.bash
ros2 --version
```

ZERG 驱动还需要 ROS2 Python 包和标准消息包。若重新编译 ZERG 工作区，使用：

```bash
cd ~/Demo/ZERG-SDK
source /opt/ros/humble/setup.bash
rosdep install --from-paths zerg_interfaces zerg_ros2_driver --ignore-src -r -y
colcon build --symlink-install
source install/setup.bash
```

### 3.3 Python 后端

```bash
cd ~/Demo/jakaA5/python——control
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

`requirements.txt` 当前包含：

- `fastapi`
- `uvicorn[standard]`
- `pyserial`
- `minimalmodbus`

JAKA SDK、ZERG SDK 和 ROS2 环境不通过 pip 安装，分别来自本地 SDK 目录和 ROS2 工作区。

### 3.4 前端

```bash
cd ~/Demo/jakaA5/python——control/frontend
npm ci
npm run build
```

主要前端依赖：React、React DOM、TypeScript、Vite、Tailwind CSS、`lucide-react`。

## 4. 环境变量和路径

`start_backend.sh` 会自动设置以下路径：

```bash
export JAKA_SDK_DIR=~/Demo/JAKA_Mini2_Python_Test/sdk
export ZERG_ROOT=~/Demo/ZERG-SDK
export LD_LIBRARY_PATH="$JAKA_SDK_DIR:$LD_LIBRARY_PATH"
export PYTHONPATH="$JAKA_SDK_DIR:$ZERG_ROOT/zerg_sdk:/usr/lib/python3/dist-packages:$PYTHONPATH"
```

可选配置：

```bash
export COORD_A5_IP=192.168.1.102
export COORD_MINI_IP=192.168.1.103
export ADP_PORT=/dev/ttyUSB1
export ADP_ADDRESS=02
export ADP_BAUD=115200
```

## 5. 设备安装与配置清单

### 5.1 JAKA A5

- [ ] 控制柜已上电，急停已复位。
- [ ] 网络掩码与电脑在同一网段，通常为 `255.255.255.0`。
- [ ] JAKA App 中确认 IP 为 `192.168.1.102`，或同步修改 `COORD_A5_IP`。
- [ ] 电脑执行 `ping 192.168.1.102` 成功。
- [ ] 控制源设置为 SDK。
- [ ] 网页连接、上电、使能均成功。

### 5.2 JAKA Mini

- [ ] 控制柜已上电，急停已复位。
- [ ] JAKA App 中确认 IP 为 `192.168.1.103`。
- [ ] 电脑执行 `ping 192.168.1.103` 成功。
- [ ] Mini 的 Home、试管上方、试管下方、点位 4/5/6 已在 `points.json` 中存在。
- [ ] Mini SDK 登录、上电、使能成功。

### 5.3 Z-ERG-20C 旋转夹爪

- [ ] ROS2 Humble 可用。
- [ ] USB-RS485 设备路径存在：

  ```bash
  readlink -f /dev/serial/by-id/usb-1a86_USB_Serial-if00-port0
  ```

- [ ] 当前串口未被其他驱动占用：

  ```bash
  fuser -v /dev/ttyUSB2
  ```

- [ ] 参数为 `115200`、`slave_id=1`。
- [ ] 页面显示 ROS2 Action 就绪。
- [ ] 初始化、启用电机成功后再启动总流程。
- [ ] 若出现 `serial response timed out after 0/2 bytes`，先检查 24V、RS485 A/B/GND 和串口占用，不要连续点击初始化。

### 5.4 ADP 移液枪

- [ ] USB-RS485 转换器识别成功：

  ```bash
  lsusb
  ls -l /dev/ttyUSB*
  ```

- [ ] 串口填写实际设备，例如 `/dev/ttyUSB1`，不能凭固定编号假设。
- [ ] 地址通常为 `02`，波特率通常为 `115200`。
- [ ] 网页连接成功。
- [ ] 初始化成功并安装 Tip。
- [ ] 手动执行空气回吸 30uL，确认移液枪状态正常。
- [ ] 再执行总流程；总流程会调用吸液 600uL 和排液 200uL x 3。

### 5.5 末端夹爪（附属）

- [ ] 通过手动控制页连接并初始化。
- [ ] 确认能够手动张开、闭合。
- [ ] 总流程会使用它夹取橙色试管、橙色盖子并放置终点。

## 6. 启动顺序

### 6.1 推荐一键启动

```bash
cd ~/Demo/jakaA5/python——control
./start_dashboard.sh
```

打开：`http://127.0.0.1:5173/`

### 6.2 分开启动

终端 1：

```bash
cd ~/Demo/jakaA5/python——control
./start_backend.sh
```

终端 2：

```bash
cd ~/Demo/jakaA5/python——control/frontend
npm run dev -- --host 0.0.0.0
```

后端 API：`http://127.0.0.1:8000`；前端：`http://127.0.0.1:5173`。

## 7. 启动前验收

```bash
curl http://127.0.0.1:8000/api/status
curl http://127.0.0.1:8000/api/transfer-subflow/state
curl http://127.0.0.1:8000/api/orange-capping/state
curl http://127.0.0.1:8000/api/combined-subflow/state
```

总流程必须显示 `status=idle`、`steps=113`。设备准备完成后，在流程编辑页顶部选择：

`总流程：紫色取液 → 橙色三管`

然后点击“开始”。首次联调建议低速、空载或使用防护措施，并逐步观察日志。

## 8. 交付前备份清单

- [ ] `python——control/` 全目录
- [ ] `JAKA_Mini2_Python_Test/sdk/` 中的 JAKA SDK 二进制和类型文件
- [ ] `ZERG-SDK/` 源码、接口、驱动和构建工作区
- [ ] `adp_rs485_check（仅检查通讯是否正常的脚本）.py`
- [ ] `adp_rs485_interactive（吸0.6ml分三批排0.2ml脚本）.py`
- [ ] `points.json`、`flows.json`
- [ ] `.venv` 不建议作为跨机器交付内容，目标机器重新创建并安装依赖
- [ ] `frontend/node_modules` 不建议作为跨机器交付内容，使用 `npm ci` 重建
- [ ] `frontend/src/` 全目录及 `index.html`、Vite/Tailwind/TypeScript 配置文件
- [ ] `frontend/public/` 静态资源（至少 `favicon.svg`、`icons.svg`）
- [ ] `frontend/src/assets/` 静态资源
- [ ] `frontend/src/components/CoordinatedWorkflowPanel.tsx`（旧流程兼容/回滚参考，可选）
- [ ] 记录现场 A5/Mini IP、ZERG 串口路径、移液枪串口、RS485 地址和波特率
- [ ] 记录 ROS2、Python、Node.js、npm 版本

## 10. 审计结论：必需与可选文件

本次按 `server.py`、`start_backend.sh`、`start_dashboard.sh`、`frontend/src/App.tsx` 的实际引用关系反向审计：

- 总流程运行必需：`server.py`、`jaka_arm.py`、`gripper.py`、`pipette.py`、`points.json`、`flows.json`、JAKA SDK、ZERG SDK/ROS2 工作区、前端构建入口和被 `App.tsx` 引用的组件。
- 移液枪独立联调：两个 `adp_rs485_*.py` 脚本，属于交付包但不是网页总流程启动必需文件。
- JAKA 单机示例、`read_current_pose.py`、`move_robot_points.py` 等测试/示例脚本：当前总流程没有调用，属于可选测试资料，不应冒充总流程运行依赖。
- `ZERG-SDK/build/`、`ZERG-SDK/install/` 可以从源码重新生成；为了现场快速交付建议一起保留，但跨机器时必须确认目标机 ROS2 版本和 Python 版本一致。

## 9. 运行日志和故障定位

后台日志默认可查看：

```bash
tail -f /tmp/jaka_a5_backend.log
```

端口占用：

```bash
ss -ltnp '( sport = :5173 or sport = :8000 )'
```

ROS2 Action：

```bash
source /opt/ros/humble/setup.bash
source ~/Demo/ZERG-SDK/install/setup.bash
ros2 action list -t
ros2 service list | grep zerg_driver
```

总流程的安全原则是：任何设备返回错误、运动到位确认超时、串口无响应或用户点击停止，都会取消当前总流程的后续阶段。
